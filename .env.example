# Copy to .env and point at your vault's plugin folder.
# The build (npm run build / npm run dev) copies main.js, manifest.json and
# styles.css here after each successful compile.
OBSIDIAN_PLUGIN_DIR=/absolute/path/to/YourVault/.obsidian/plugins/obsictionary
